import React from 'react'

let div: HTMLDivElement
let p: HTMLParagraphElement
let img: HTMLImageElement
// ...

// If we want to type children for a component we use
let node: React.ReactNode

let pointerEvent: React.PointerEvent
let mouseEvent: React.MouseEvent
let keyboardEvent: React.KeyboardEvent
let formEvent: React.FormEvent
// ...

let clickHandler: React.MouseEventHandler<HTMLButtonElement>
